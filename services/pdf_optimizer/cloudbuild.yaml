steps:
  - name: 'gcr.io/cloud-builders/docker'
    dir: 'services/pdf_optimizer'
    args: 
      - 'build'
      - '-t'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/pdf-optimizer:latest'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    dir: 'services/pdf_optimizer'
    args: 
      - 'push'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/pdf-optimizer:latest'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    dir: 'services/pdf_optimizer'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'pdf-optimizer'
      - '--image'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/pdf-optimizer:latest'
      - '--region'
      - 'asia-northeast1'
      - '--allow-unauthenticated'
      - '--memory'
      - '512Mi'

images:
  - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/pdf-optimizer:latest'

options:
  logging: CLOUD_LOGGING_ONLY
  substitutionOption: 'ALLOW_LOOSE'
