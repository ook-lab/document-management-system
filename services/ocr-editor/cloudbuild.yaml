steps:
  - name: 'gcr.io/cloud-builders/docker'
    dir: 'services/ocr-editor'
    args: 
      - 'build'
      - '-t'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/ocr-editor:latest'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    dir: 'services/ocr-editor'
    args: 
      - 'push'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/ocr-editor:latest'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    dir: 'services/ocr-editor'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'ocr-editor'
      - '--image'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/ocr-editor:latest'
      - '--region'
      - 'asia-northeast1'
      - '--allow-unauthenticated'
      - '--memory'
      - '512Mi'
      - '--set-env-vars=GOOGLE_AI_API_KEY=${_GOOGLE_AI_API_KEY}'

images:
  - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/ocr-editor:latest'

options:
  logging: CLOUD_LOGGING_ONLY
  substitutionOption: 'ALLOW_LOOSE'
