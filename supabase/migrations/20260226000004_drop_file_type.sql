-- ============================================================
-- file_type カラム削除
-- ============================================================
--
-- 【背景】
-- file_type カラムは不要と判断。
-- 既存インデックス・参照を含めて完全削除する。
--
-- ============================================================

ALTER TABLE "Rawdata_FILE_AND_MAIL" DROP COLUMN IF EXISTS file_type;

-- ============================================================
-- 完了ログ
-- ============================================================

DO $$
BEGIN
    RAISE NOTICE '✅ 20260226000004_drop_file_type.sql 適用完了';
    RAISE NOTICE '  - Rawdata_FILE_AND_MAIL: file_type カラム削除';
END $$;
