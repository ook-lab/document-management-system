# Dual Editor UI

リアルタイムMarkdown→HTMLプレビュー機能を持つReactコンポーネント

## 📋 概要

**DualEditor**は、左右2分割のパネルで構成されるMarkdownエディタです:
- **左パネル (Editor A)**: Markdown入力エリア
- **右パネル (Editor B)**: HTMLプレビュー
- **リアルタイム同期**: 入力と同時にプレビューが更新

## 🚀 セットアップ

### 1. 依存関係のインストール

```bash
npm install
```

### 2. 開発サーバーの起動

```bash
npm run dev
```

ブラウザで `http://localhost:5173` を開くと、Dual Editorが表示されます。

### 3. ビルド

```bash
npm run build
```

## 🛠️ 技術スタック

- **React** 18.3.1
- **TypeScript** 5.6.3
- **Vite** 5.4.11 (ビルドツール)
- **marked** 11.1.1 (Markdownパーサー)

## 📂 ファイル構造

```
frontend/
├── src/
│   ├── components/
│   │   ├── DualEditor.tsx           # メインコンポーネント
│   │   └── DualEditor.module.css    # スタイル定義
│   ├── App.tsx                       # アプリケーションエントリーポイント
│   ├── App.css                       # グローバルスタイル
│   └── main.tsx                      # ReactDOM.render
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

## 🎨 主要機能

### 1. リアルタイム同期
- Markdownテキストの変更を即座に検知
- `useMemo`フックによるパフォーマンス最適化
- 一方向データバインディング (Input → Preview)

### 2. Markdownサポート
- **GitHub Flavored Markdown (GFM)** 対応
- 見出し、リスト、強調、コードブロック、テーブルなど全てサポート
- 自動改行変換

### 3. スタイリング
- CSS Modulesによるスコープ分離
- レスポンシブデザイン (モバイル対応)
- カスタムスクロールバー
- グラデーションヘッダー

## 🔒 セキュリティ考慮事項

### XSS対策

コンポーネントは`dangerouslySetInnerHTML`を使用しているため、以下の点に注意してください:

⚠️ **警告**: 信頼できないソースからのMarkdownを受け入れる場合、XSS攻撃のリスクがあります。

#### 推奨対策:
1. **DOMPurify**等のサニタイゼーションライブラリの導入
2. Content Security Policy (CSP) ヘッダーの設定
3. 信頼できないユーザー入力の検証

#### DOMPurifyの導入例:

```bash
npm install dompurify
npm install --save-dev @types/dompurify
```

```typescript
import DOMPurify from 'dompurify';

const htmlContent = useMemo(() => {
  const rawHtml = marked.parse(markdown);
  return DOMPurify.sanitize(rawHtml);
}, [markdown]);
```

## 📝 使用例

### 基本的な使用

```tsx
import DualEditor from './components/DualEditor';

function App() {
  return (
    <div className="App">
      <DualEditor />
    </div>
  );
}
```

### カスタム初期コンテンツ

`DualEditor.tsx`の`INITIAL_MARKDOWN`定数を変更:

```typescript
const INITIAL_MARKDOWN = `# Your Custom Title
Your custom initial content here...`;
```

## 🎯 将来の拡張機能案

- [ ] エクスポート機能 (Markdown/HTML/PDF)
- [ ] シンタックスハイライト (コードブロック用)
- [ ] テーマ切り替え (ダーク/ライトモード)
- [ ] ローカルストレージによる自動保存
- [ ] ショートカットキー対応
- [ ] 画像アップロード機能
- [ ] Markdown記法ツールバー

## 📄 ライセンス

MIT License

## 👨‍💻 開発者

Generated with Claude Code

---

**Enjoy your Markdown editing experience!** ✨
